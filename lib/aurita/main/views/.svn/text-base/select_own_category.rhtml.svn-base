<div>

  <select name="<%= attrib_name %>" class="lore">

<% categories.each do |cat| %>
   <option value="<%= cat.category_id %>" <%= 'selected' if cat.is_private? %>><%= cat.category_name %></option>
<% end %>

  </select>

</div