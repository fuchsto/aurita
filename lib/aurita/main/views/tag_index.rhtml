<div>
<% displayed = Array.new
   tags.each do |entry| 
   if !displayed.include? entry.tag then
   displayed << entry.tag
%>

<font class="link" onclick="Cuba.load({ element: 'app_main_content', action: 'App_Main/find_by_tag/tag=<%= entry.tag.to_s.gsub('"','') %>'});" >
  <%= entry.tag %> (<%= entry.relevance %>)
</font>

<% end 
   end
%>

</div>
